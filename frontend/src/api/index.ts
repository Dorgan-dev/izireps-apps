import {
  devicesApi,
  bookingsApi,
  sessionsApi,
  transactionsApi,
  customerAuthApi,
} from '../services/api';

export const deviceApi = devicesApi;
export const bookingApi = bookingsApi;
export const sessionApi = sessionsApi;
export const transactionApi = transactionsApi;
export const customerApi = customerAuthApi;