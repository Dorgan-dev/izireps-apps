import AppRouter from "./router";

export default function App() {
  // const token = useAuthStore((s) => s.token);

  // useEffect(() => {
  //   if (token) {
  //     authApi.me();
  //   }
  // }, []);
  return <AppRouter />;
}
